#!/bin/sh
# REMOVED: This script is no longer needed. The project uses Neon instead of Supabase.
# Use scripts/apply-migrations.js to set up the database schema.
echo "setup-supabase.sh has been removed. Use Neon (DATABASE_URL) instead."
exit 1
